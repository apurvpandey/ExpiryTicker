<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark variant — main theme unchanged (DayNight handles it) -->
    <style name="Theme.ExpiryTicker" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Dark splash: brand indigo background kept for consistency -->
    <style name="Theme.ExpiryTicker.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#0D1B3E</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="windowSplashScreenIconBackgroundColor">#0D1B3E</item>
        <item name="postSplashScreenTheme">@style/Theme.ExpiryTicker</item>
    </style>
</resources>
